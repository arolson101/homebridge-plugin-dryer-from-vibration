{
  "pluginAlias": "DryerFromVibration",
  "pluginType": "platform",
  "singular": false,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "Dryer Sensor"
      },
      "numberOfEvents": {
        "title": "Number of Events (N)",
        "type": "number",
        "required": true,
        "default": "4"
      },
      "onTimeSpanSec": {
        "title": "Trigger On after N events within timespan (s)",
        "type": "number",
        "required": true,
        "default": "300"
      },
      "offTimeSpanSec": {
        "title": "Trigger Off after no events for timespan (s)",
        "type": "number",
        "required": true,
        "default": "300"
      }
    }
  }
}
